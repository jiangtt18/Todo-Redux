const todoReducer = (state={}, action) => {
    Object.freeze(state);
    switch(action.type){
        // case :
        //     break;
        // case:
        //     break;
        default:
            return state;
    }
};

export default todoReducer;